version: '3.8'

services:
  backend:
    build: ./backend
    image: 127.0.0.1:5001/backend:latest  # Tag pour Nexus Docker-hosted
    container_name: flask-backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

  frontend:
    build: ./frontend
    image: 127.0.0.1:5001/frontend:latest  # Tag pour Nexus Docker-hosted
    container_name: react-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - "8081:8081" # Interface web
      - "5001:5001" # Docker registry
    volumes:
      - nexus-data:/nexus-data
    restart: unless-stopped

volumes:
  nexus-data:
